#include "lists.h"

/**
  * free_listint_safe - fills memory with a constant byte
  * @h: is the owner of the dog
  * Return: a number
  */
size_t free_listint_safe(listint_t **h)
{
	size_t i = 0;

	(void) h;
	return (i);
}
