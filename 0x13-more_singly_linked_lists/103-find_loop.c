#include "lists.h"

/**
  * find_listint_loop - fills memory with a constant byte
  * @head: is the owner of the dog
  * Return: a number
  */
listint_t *find_listint_loop(listint_t *head)
{
	listint_t *headcopy;

	headcopy = head;
	return (headcopy);
}
